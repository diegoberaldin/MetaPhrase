package com.github.diegoberaldin.metaphrase.domain.project.data

/**
 * Message filter (used for message filtering)
 *
 * @constructor Create empty constructor for translation unit type filter
 */
enum class TranslationUnitTypeFilter {
    ALL,
    UNTRANSLATED,
    TRANSLATABLE,
}
